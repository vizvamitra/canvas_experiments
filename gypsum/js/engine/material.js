Engine.Material = class Material {
  constructor(ambient, diffuse, specular, shininess) {
    this.ambient = ambient
    this.diffuse = diffuse
    this.specular = specular
    this.shininess = shininess
  }
}
